"""Vision helpers for go2_survey.

Placeholders for future capabilities:
    frames.py — pull still frames from the Go2 WebRTC video track
    geotag.py — stamp frames with GPS + heading for downstream geotagging
"""
