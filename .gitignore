# Python build & install artifacts
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/

# Editor cruft
*.swp

# Large binary model files
*.pt
*.pth
*.whl

# Legacy artifacts from the pre-refactor repo (root-only — the
# per-mission `dev/missions/<m>/runs/` dirs are intentionally tracked).
/runs/
results/
mapping.txt

# NOTE: per-mission run dirs under dev/missions/*/runs/ and dev/archive/
# are intentionally tracked — they are historical run data + captures,
# not build output. (Prior to v0.10.0 these lived under logs/ and
# captures/; see dev/scripts/migrate_to_runs_layout.py for the rename.)
